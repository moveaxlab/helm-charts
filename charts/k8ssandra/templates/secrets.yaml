apiVersion: v1
kind: Secret
metadata:
  name: cassandra-backups-secret
type: Opaque
stringData:
 medusa_s3_credentials: |-
    [default]
    aws_access_key_id = {{ .Values.secrets.awssecretaccesskey  }}
    aws_secret_access_key = {{ .Values.secrets.awsaccesskeyid  }}